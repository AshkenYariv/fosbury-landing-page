# Where the "Request access" CTAs point. Defaults to the Calendly link to Jariv.
NEXT_PUBLIC_BOOK_DEMO_URL=

# Umami analytics website ID. Defaults to a shared dev site if unset; override
# in production (Vercel) so traffic lands in this project's Umami dashboard.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
